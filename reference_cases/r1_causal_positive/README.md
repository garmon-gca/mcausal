# R1 — causal positive

Live module: `mcausal.reference.r1_gain_causal`.

Function-preserving adapter/k, scale·k. ResidualMatch should support gain as mediator.

```text
mcausal ref
```

Expect: `R1 CAUSAL POSITIVE: PASS`
